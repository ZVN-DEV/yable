// @zvndev/yable-core — Test helpers barrel
//
// Re-exports shared test fixtures so consumers can import everything from
// one place, e.g.:
//
//   import { makeTableState, makeTestTable } from '../__tests__/helpers'
//
// (The exact relative path depends on where the test file lives.)

export * from './makeTableState'
